#include <stdio.h>

// Используем puts() для вывода сообщения на экран.
// puts() автоматически добавляет перевод строки в конце
int main(int argc[], char *argv[], char *envp[]) {
    puts("Hello, world!");
    return 0;
}
